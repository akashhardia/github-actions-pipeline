ja:
  activerecord:
    errors:
      models:
        campaign:
          attributes:
            start_at:
              start_at_over_end_at: "はより後の値に指定できません"
            approved_at:
              available_at_over_end_at: "は予定終了日より後の値に指定できません"
            terminated_at:
              terminated_at_over_end_at: "は予定終了日より後に指定できません"
            code:
              invalid: は10文字以下の半角英数字が使えます
